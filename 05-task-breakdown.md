# Task Breakdown — Urutan Pengerjaan

Kerjakan **berurutan dari atas ke bawah**. Jangan lompat ke task berikutnya sebelum task sebelumnya lolos "Definition of Done". Setiap task idealnya 1 sesi kerja/1 commit.

Referensi dokumen lain:
- Detail requirement per fitur → `02-requirements-prd.md`
- Schema database → `03-database-migration.sql`
- Aturan koding → `04-coding-standards.md`
- Arsitektur & folder structure → `01-rancangan-arsitektur-kas-platform.md`

---

## TASK 0 — Setup Proyek
**Dependency:** tidak ada (task pertama)

- [x] Init Next.js project (App Router, TypeScript, Tailwind) di dalam repo GitHub yang sudah dibuat
- [x] Install & setup shadcn/ui
- [x] Install `@supabase/supabase-js`, `@supabase/ssr`, `react-hook-form`, `zod`
- [x] Buat project Supabase baru, jalankan seluruh isi `03-database-migration.sql` di SQL Editor
- [x] Setup `.env.local` dengan `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- [x] Buat `lib/supabase/client.ts` dan `lib/supabase/server.ts` sesuai dokumentasi resmi `@supabase/ssr`
- [x] Connect repo ke Vercel, deploy versi kosong untuk pastikan pipeline jalan
- [x] Set environment variables yang sama di Vercel

**Definition of Done:** project bisa di-deploy ke Vercel tanpa error, walau belum ada fitur apa-apa.

---

## TASK 1 — Autentikasi Dasar
**Dependency:** Task 0
**Requirement terkait:** US-1.1, US-1.2, US-1.3, US-1.4

- [x] Halaman `/login` dan `/register`
- [x] Proxy (`proxy.ts`, Next 16 pengganti `middleware.ts`) yang redirect ke `/login` kalau akses `/org/*` tanpa session
- [x] Tombol logout
- [x] Halaman reset password

**Definition of Done:** bisa register, login, logout, reset password end-to-end di local dev. Session bertahan setelah refresh.

---

## TASK 2 — Onboarding & Buat Organisasi
**Dependency:** Task 1
**Requirement terkait:** US-2.1

- [x] Halaman onboarding untuk user yang belum punya organisasi
- [x] Form buat organisasi (nama, auto-slug)
- [x] Setelah submit, verifikasi trigger database (`trg_add_creator_as_owner`, `trg_create_default_categories`) jalan dengan benar

**Definition of Done:** user baru register → diarahkan ke onboarding → buat organisasi → otomatis jadi owner → kategori default sudah ada.

---

## TASK 3 — Layout Dashboard & Organization Switcher
**Dependency:** Task 2
**Requirement terkait:** US-2.2, US-2.3

- [x] Layout dashboard dengan navbar (desktop) dan bottom nav (mobile)
- [x] Organization switcher di navbar
- [x] Route `/org/[slug]/...` dengan layout yang cek membership user ke organisasi tsb (403 kalau bukan anggota)
- [x] Tombol "buat organisasi baru" dari switcher

**Definition of Done:** user dengan 2+ organisasi bisa pindah-pindah lewat switcher, URL selalu sinkron dengan organisasi aktif. Akses ke slug organisasi yang bukan miliknya menghasilkan 403, bukan crash.

**Test wajib sebelum lanjut:** buat 2 akun uji, masing-masing bikin organisasi sendiri, pastikan tidak bisa akses slug organisasi akun lain lewat URL manual.

---

## TASK 4 — CRUD Transaksi
**Dependency:** Task 3
**Requirement terkait:** US-3.1, US-3.2, US-3.3

- [x] Form tambah transaksi (dropdown kategori dari organisasi aktif)
- [x] List transaksi + filter (tanggal, kategori, jenis)
- [x] Edit & hapus transaksi (dengan konfirmasi dialog)
- [x] Role check: sembunyikan tombol tambah/edit/hapus untuk role `viewer`

**Definition of Done:** owner/treasurer bisa CRUD penuh, viewer hanya bisa lihat (dicoba lewat UI maupun langsung manipulasi request — keduanya harus ketolak).

**Test wajib sebelum lanjut:** login sebagai viewer, coba akses form tambah transaksi langsung via URL, pastikan tidak bisa submit (RLS harus menolak di level database, bukan cuma disembunyikan di UI).

---

## TASK 5 — Kelola Kategori
**Dependency:** Task 4
**Requirement terkait:** US-3.4

- [x] Halaman kelola kategori (tambah/edit/hapus, khusus owner/treasurer)
- [x] Handling kategori yang masih dipakai transaksi (soft delete via `is_deleted`)

**Definition of Done:** kategori custom bisa ditambah, kategori yang sudah dipakai transaksi tidak bisa dihapus permanen (pakai soft delete).

---

## TASK 6 — Dashboard & Laporan
**Dependency:** Task 4
**Requirement terkait:** US-4.1, US-4.2

- [x] Kartu ringkasan saldo di dashboard
- [x] Ringkasan bulan berjalan
- [x] Halaman laporan bulanan dengan filter bulan/tahun + breakdown per kategori

**Definition of Done:** angka di dashboard sesuai dengan data transaksi aktual organisasi aktif, ter-update real-time setelah tambah/edit/hapus transaksi.

---

## TASK 7 — Undangan & Kelola Anggota
**Dependency:** Task 3
**Requirement terkait:** US-5.1, US-5.2, US-5.3, US-5.4

- [x] Form undang anggota via email (email + role) khusus owner
- [x] Integrasi `supabase.auth.admin.inviteUserByEmail()` (server-side, pakai service role key)
- [x] Halaman terima undangan (accept invite flow)
- [x] Form daftarkan anggota manual (nama, email, password sementara, role) — pakai `supabase.auth.admin.createUser()` di API route server-side, TIDAK boleh dipanggil dari client
- [x] Tampilkan password sementara sekali ke owner setelah user manual dibuat sukses
- [x] Halaman "ganti password wajib" untuk user yang login pertama kali dari akun yang dibuat manual
- [x] Halaman kelola anggota (list, ubah role, hapus anggota) khusus owner — tampilkan juga asal akun (diundang via email / didaftarkan manual)

**Definition of Done:** owner punya 2 cara tambah anggota — via email (US-5.1/5.2) dan manual tanpa email (US-5.4) — keduanya menghasilkan anggota dengan role benar di `organization_members`. Rate limit 2/jam untuk jalur email ditangani dengan pesan error jelas, bukan silent fail. Jalur manual sama sekali tidak terpengaruh limit email karena tidak mengirim email.

---

## TASK 8 — Mobile Polish & PWA
**Dependency:** semua task di atas selesai
**Requirement terkait:** Non-Functional Requirements (mobile-first)

- [x] Audit ulang SEMUA halaman di viewport 375px
- [x] Pastikan touch target minimal 44x44px di semua tombol/link
- [x] `inputMode="numeric"` di semua field nominal
- [x] Setup `manifest.json` + service worker dasar untuk PWA (add to home screen)

**Definition of Done:** aplikasi terasa nyaman dipakai di HP sungguhan (bukan cuma responsive di DevTools), bisa di-"Add to Home Screen".

---

## TASK 9 — Testing Isolasi Data & Security Final Check
**Dependency:** semua task di atas selesai

- [x] Buat 3 akun uji: masing-masing di organisasi berbeda, lalu 1 akun jadi anggota di 2 organisasi
- [x] Pastikan tidak ada kebocoran data antar organisasi di SEMUA fitur (dashboard, transaksi, kategori, laporan, anggota)
- [x] Cek tidak ada `service_role` key yang ke-expose di client-side (inspect network tab & bundle)
- [x] Cek semua route dashboard butuh auth (tidak bisa diakses tanpa login)

**Definition of Done:** checklist keamanan di section 11 dokumen arsitektur semua tercentang.

---

## TASK 10 — Deploy Production
**Dependency:** Task 9

- [x] Final deploy ke Vercel production
- [x] Smoke test semua fitur di production (bukan cuma local dev)
- [ ] (Opsional) setup custom domain kalau sudah tersedia

**Definition of Done:** aplikasi bisa diakses publik, siap dipakai user sungguhan (kamu, istri, teman).

---

## Pasca-TASK 10 (post-deploy — tambahan yang sudah selesai)

Perubahan berikut dikerjakan setelah production live, sebagai maintenance/fitur tambahan yang disetujui user:

- [x] Fix bug: navigasi desktop hilang di layar besar (`BottomNav` awalnya `md:hidden`) → buat `components/desktop-nav.tsx` (baris kedua header)
- [x] Fitur: tema kawaii pastel untuk user tertentu (istri) vs netral untuk warga komplek → multi-tema
- [x] Fitur: **tema per-akun (5 tema: Klasik, Kawaii, Ocean, Forest, Sunrise)** — pilihan tersimpan di `auth.users.user_metadata.theme`, sinkron lintas perangkat (`components/theme-picker.tsx` + `components/theme-setter.tsx`)
- [x] Fix UI: dot warna dropdown tema terpotong lengkungan → padding `p-2` di `SelectContent`
- [x] Fix UI: dropdown Select menampilkan label alih-alih value mentah (`items` auto-derive dari children di `components/ui/select.tsx`) + padding dropdown agar opsi tidak terpotong sudut membulat
- [x] Fitur: indikator loading (spinner `Loader2` + `disabled`) di semua tombol aksi form (login, register, reset password, update password, transaksi, kategori, anggota, undangan, organisasi, logout, tema)
- [x] Fix UX: tombol login/register tetap tampil loading sampai halaman tujuan siap (state `redirecting`) — hilangkan kesan "proses selesai tapi belum kelempar"
- [x] Fitur: indikator loading saat pindah menu — skeleton konten (`org/[slug]/loading.tsx` + `components/ui/skeleton.tsx`) dan spinner di link navigasi (`components/nav-link-icon.tsx` via `useLinkStatus`)
- [x] Doc sync: README + dokumen arsitektur/PRD/coding standards/task breakdown di-update agar konsisten dengan kondisi sekarang
- [x] Fitur: **tambah anggota existing** (US-5.4) — email yang sudah punya akun KasKita bisa langsung ditambahkan sebagai anggota (pilih role, tanpa password baru) lewat mode "Tambah anggota existing" di dialog "Daftarkan anggota", atau tombol konfirmasi saat daftar manual kena error "email sudah terdaftar"; API `/api/members` punya mode `existing` (cari user by email via service client, cek sudah-anggota → 409, lalu insert `organization_members`); pesan 409 undangan diarahkan ke fitur ini
- [x] Fix US-1.3: **logout accessible dari mobile** — item "Keluar" (ikon + spinner saat proses) ditambahkan sebagai item ke-6 di `BottomNav` (sebelumnya tombol logout hanya muncul di `md:` ke atas, user mobile tidak bisa logout dari dashboard)
- [x] Doc sync: **semua checklist acceptance criteria di `02-requirements-prd.md` di-centang sesuai kondisi nyata** — diverifikasi ulang ke kode satu per satu (register, login, logout, reset, organisasi, switcher, transaksi, filter, pagination, kategori, dashboard, laporan, undangan, kelola anggota). Tersisa 2 item yang memang belum dibuat: (1) US-3.1 upload foto bukti transaksi — opsional, belum dikerjakan, kolom `receipt_url` menganggur; (2) US-4.2 export PDF — memang fase 2.
- [x] Fix bug: **data basi di HP setelah refresh (anggota/kategori "ilang" padahal sudah tersimpan di Supabase)** — penyebab: service worker `public/sw.js` memakai strategi cache-first untuk SEMUA GET same-origin non-navigasi, termasuk `/api/members` dan payload RSC, sehingga refresh di-serve dari cache lama (logout + hard refresh terlihat "memperbaiki" karena mem-bypass SW). Perbaikan: `sw.js` v2 hanya cache-first untuk aset statis ber-hash (`/_next/static/`), navigasi network-first (fallback cache hanya saat offline), dan API/RSC/data **selalu dari jaringan (tidak pernah di-cache)** + `Cache-Control: no-store` pada GET `/api/members` dan `/api/invitations/accept`. Cache lama (`kaskita-v1`) dihapus saat activate.
- [x] Audit menyeluruh: **tidak ada pola "data masuk tapi UI tampak error/basi" lain** — semua mutasi sudah tanpa `.insert().select()` (hindari RLS RETURNING gotcha), semua flow sukses memanggil `router.refresh()` (server components) atau reload via API (halaman anggota), semua POST tak pernah di-cache, 2 GET data sudah no-store. Satu penguatan kecil: tombol "Hapus" di dialog konfirmasi transaksi & kategori kini punya state loading (spinner + disabled) untuk mencegah double-tap yang terkesan error.
- [x] Fix mobile: **konten paling bawah tertutup bottom nav** — `<main>` di org layout kini punya padding bawah `pb-[calc(5rem+env(safe-area-inset-bottom))]` (mobile) sehingga card/tombol terakhir bisa discroll melewati nav, dan `BottomNav` diberi `env(safe-area-inset-bottom)` agar ikon tidak tertutup home indicator iPhone.
- [x] Fix laporan (akuntansi kumulatif): **laporan bulanan tidak boleh terkesan "reset ke nol" per bulan** — sebelumnya card "Saldo akhir bulan" menampilkan `income - expense` bulan itu saja (mislabel). Sekarang `MonthTotals` punya `openingBalance` & `closingBalance` (`lib/types.ts`); `reports/page.tsx` menghitung saldo awal = kumulatif semua transaksi sebelum tanggal 1 bulan terpilih, lalu saldo akhir = saldo awal + selisih bulan itu; `reports-view.tsx` menampilkan urutan: Saldo awal bulan → pemasukan → pengeluaran → selisih bulan ini → Saldo akhir bulan. Dokumen PRD US-4.2 di-update (saldo awal/akhir kumulatif, menyambung antar bulan).
- [x] Konsistensi saldo kumulatif: **dashboard "Bulan ini" kini sejalan dengan laporan** — `dashboard/page.tsx` menambah saldo awal bulan (kumulatif sebelum tanggal 1 bulan berjalan) dan saldo akhir bulan (= saldo saat ini, kumulatif), label "Selisih" diubah jadi "Selisih bulan ini". Struktur card bulan berjalan di dashboard identik dengan laporan bulanan (Saldo awal → Pemasukan → Pengeluaran → Selisih → Saldo akhir). PRD US-4.1, arsitektur (komentar struktur folder), README di-update. Tidak ada tempat lain yang menampilkan saldo (transaksi/kategori/anggota hanya list tanpa saldo, tidak perlu diubah).
- [x] Fix akses role (UX): **menu Anggota & Pengaturan disembunyikan untuk non-owner** — sebelumnya nav (`BottomNav`/`DesktopNav`) menampilkan semua menu padahal halaman members sudah memblokir non-owner (`Forbidden`), dan halaman settings belum punya role check sama sekali (placeholder). Sekarang: layout `org/[slug]` membaca role dari `organization_members` dan mengirimnya ke nav; item `members` & `settings` difilter (hanya `owner`); grid `BottomNav` menyesuaikan jumlah kolom dinamis; halaman `settings/page.tsx` kini juga memblokir non-owner (konsisten dengan members). Defense-in-depth: menu disembunyikan DAN route tetap ditolak di server. PRD US-5.3 + arsitektur (deskripsi role) di-update.
- [x] Fix UX ganti password: **pesan error jelas saat password baru sama dengan password sementara** — `update-password/page.tsx` mendeteksi error GoTrue ("different from old password") dan menampilkan "Password baru tidak boleh sama dengan password yang sedang dipakai (password sementara). Pilih password yang berbeda." alih-alih pesan gagal generik.
- [x] Fitur: **kelola akun anggota oleh owner** — tombol "Kelola" per anggota bendahara/viewer membuka `member-manage-dialog.tsx` berisi 3 aksi: (1) **atur ulang password sementara** — `admin.updateUserById` dengan `user_metadata.must_change_password=true` sehingga anggota diminta ganti password saat login berikutnya, password baru ditampilkan untuk disampaikan aman; (2) **ganti email** — `admin.updateUserById` dengan `email` + `email_confirm=true`, deteksi 409 jika email sudah dipakai akun lain; (3) **nonaktifkan/aktifkan akun** — ban via `ban_duration: '876000h'` / unban `'none'`, badge "Nonaktif" muncul di daftar anggota. Semua aksi lewat POST `/api/members` (`resetPassword`/`changeEmail`/`setActive`), hanya untuk role bendahara/viewer & bukan diri sendiri (server-check via helper `findManageableMember` + `getRequester` owner-only). `MemberRow` kini punya `banned_until`. README, PRD (Fitur Tambahan), arsitektur (komentar API route) di-update.
- [x] Fitur: **putuskan semua sesi (kick session)** — section ke-4 di dialog Kelola. `POST /api/members` mode `revokeSessions` menghapus semua baris `auth.sessions` milik user via service client (`admin.from("auth.sessions").delete().eq("user_id", ...)`), sehingga seluruh refresh token tidak berlaku seketika (access token JWT lama kedaluwarsa otomatis maks. ±1 jam). UI menampilkan konfirmasi + note bahwa refresh token langsung mati & access token lama ±1 jam, hasil "Semua sesi X diputus". Dipasangkan dengan atur ulang password untuk prosedur akun kena hack. Perlu diperhatikan: ban (`setActive`) TIDAK mencabut sesi aktif (perilaku Supabase), jadi kick session adalah langkah terpisah. README, PRD (Fitur Tambahan), arsitektur (komentar API route) di-update.
- [x] Fix UX desktop: **org switcher & tombol keluar** — dropdown `SelectContent` memakai `w-(--anchor-width)` sehingga selebar trigger (176px) dan nama org panjang ketimpa ikon centang; trigger kini `sm:max-w-64` (nama org di PC tidak kepotong) dan content `min-w-64`, nama org dibungkus `min-w-0 flex-1 truncate` (nama panjang terpotong elipsis rapi, bukan ketimpa centang). Tombol Keluar di header PC: basis `w-full` tidak di-override sehingga lebar aneh — kini `w-auto shrink-0` di layout.
- [x] Fix UX mobile transaksi: **list item & filter tanggal** — nominal di kanan kini `whitespace-nowrap` + kolom kanan `shrink-0` (Rp + angka selalu satu baris), sementara catatan (`description`) tidak lagi `truncate` tapi `break-words` sehingga turun ke baris bawah bila panjang. Filter tanggal diberi label terlihat "Dari tanggal" & "Sampai tanggal" (+ label "Jenis"/"Kategori" untuk konsistensi grid md:grid-cols-4), menggantikan `aria-label` yang tak terlihat di mobile.
- [x] Fitur: **export PDF laporan bulanan** (US-4.2, owner/bendahara) — tombol "Export PDF" di halaman Laporan (`reports-view.tsx`) mengunduh PDF berisi ringkasan (saldo awal bulan, pemasukan, pengeluaran, selisih, saldo akhir), rincian per kategori, dan daftar transaksi detail per tanggal (tanggal, kategori, keterangan, pemasukan/pengeluaran) untuk dibagikan via WhatsApp. Dihasilkan server-side di `GET /api/reports?orgId&month&year` memakai **pdfmake 0.3.11** (dependency baru): font Roboto di-embed dari base64 `pdfmake/build/vfs_fonts` ke `virtualfs` lalu `setFonts` (`lib/pdf/fonts.ts`), docDefinition + `generateReportPdf()` di `lib/pdf/report.ts`, file `laporan-kas-<slug>-<tahun>-<bulan>.pdf` dengan `Content-Type: application/pdf` + `Cache-Control: no-store`. **Otorisasi**: `getRequester(orgId, ["owner","treasurer"])` — `getRequester` di `lib/api-helpers.ts` kini menerima daftar role (default owner, backward compatible). Data dibaca lewat client RLS user (bukan service_role) sebagai defense-in-depth; agregasi saldo/breakdown dipindah ke `lib/reports-data.ts` (`summarizeMonth`) agar page & API tidak menduplikasi logika. Gotcha teknis: `import * as pdfmake` dari modul CJS gagal saat build (Turbopack membekukan namespace → `this.fonts` tak bisa di-assign); solusi default-import CJS + augmentasi tipe `types/pdfmake.d.ts` (types resmi @types/pdfmake 0.3.3 untuk API 0.3.x). README, PRD (US-4.2 + Fitur Tambahan), arsitektur (struktur API/lib) di-update.
- [x] Fitur lanjutan PDF: **logo KasKita di header laporan** — `lib/pdf/logo.png` (copy dari `public/logo.png`, 614×614) dibaca server-side via `readFileSync(new URL("./logo.png", import.meta.url))` lalu di-base64 jadi data URL (`LOGO_DATA_URL` di `lib/pdf/fonts.ts`); docDefinition kini punya `images.logo` + baris header `columns` (logo 64×64 di kiri, judul/org/periode/tanggal di kanan). `new URL` di-trace Turbopack ke `.next/server/assets/logo.<hash>.png` (terbundle otomatis, aman untuk Vercel; diverifikasi build + produksi). Gotcha: registrasi logo langsung ke `virtualfs.storage` (pola font) GAGAL — `readFileSync` pdfmake memanggil `normalizeFilename` pada key non-string sehingga buffer logo dibaca ulang sebagai path; solusi final data URL karena di-decode `Buffer.from` internal pdfmake. Terverifikasi: PDF valid `%PDF-1.3` 132KB (sebelumnya 21KB) dengan XObject FlateDecode PNG.
- [x] Fix UX mobile: **input tanggal kosong di HP** — di browser mobile (iOS/Android) `<input type="date">` tidak menampilkan placeholder native sehingga field tampak kosong; di PC tampil `dd/mm/yyyy` native. Solusi komponen baru `components/date-input.tsx` (`DateInput`): membungkus Input + overlay hint `dd/mm/yyyy` (pointer-events-none, hanya muncul saat nilai kosong, posisi kiri tengah) dan menyembunyikan placeholder webkit via `[&::-webkit-datetime-edit]:text-transparent` agar tidak dobel teks di Chrome/Edge/Safari desktop. Dipakai di filter transaksi "Dari tanggal"/"Sampai tanggal" (`transactions-view.tsx`) dan field Tanggal form transaksi (`transaction-form-dialog.tsx`, nilai via `watch("transaction_date")`).
- [x] Fix UX loading: **indikator skeleton saat filter dipilih** — sebelumnya saat ganti filter/pagination (transaksi) atau bulan/tahun (laporan) `router.push` berubah URL tanpa umpan balik visual (halaman server tidak re-render seketika). Solusi `useTransition` + `startTransition` membungkus `router.push`: `isPending` true selama RSC data baru diambil. Transaksi: daftar diganti `TransactionListSkeleton` (4 kartu skeleton, pagination disembunyikan, `aria-busy`), filter/tombol tetap terlihat. Laporan: dua card ringkasan diganti `ReportsSkeleton` (header + baris angka), select bulan/tahun + tombol Export PDF tetap terlihat. Anggota: teks "Memuat anggota..." diganti skeleton list (header + 4 baris kartu) dan `loadMembers()` kini set loading juga saat reload setelah ubah peran/hapus/undang. Dashboard & Kategori tidak pakai searchParams (hanya skeleton `loading.tsx` saat pindah menu — sudah cukup); Settings statis. Lint 0 error, build sukses.
- [x] Fitur: **export PDF untuk SEMUA role** (owner/co-owner/bendahara/viewer) — tombol "Export PDF" di halaman Laporan tidak lagi disembunyikan untuk viewer (laporan memang bisa dilihat semua role; export hanya unduhan read-only). `GET /api/reports` otorisasi diperluas ke `["owner","co_owner","treasurer","viewer"]`; prop `canManage` dihapus dari `ReportsView` dan halaman `reports`. Logika `summarizeMonth` tidak berubah.
- [x] Fitur: **role co-owner** — role baru (nilai DB `co_owner`, label "Co-owner") yang berperilaku seperti owner di organisasinya, kecuali **tidak bisa membuat organisasi baru** dan **tidak bisa menghapus organisasi**. Cakupan:
  - Migration `supabase/migrations/202608090001_co_owner_role_and_org_creation.sql` (file BARU — file `03-database-migration.sql` yang sudah dijalankan tidak diubah): perluas check constraint `organization_members_role_check` → +`'co_owner'`; tambah fungsi `can_create_organization()`; ubah policy `insert_org` → `created_by = auth.uid() AND can_create_organization()` (hanya owner / user tanpa org yang bisa buat); ubah policy update org, kelola member, undangan, transaksi, & kategori agar menerima `co_owner`; policy delete org tetap owner-only.
  - API `/api/members`: `getRequester` untuk GET/POST/PATCH/DELETE kini `["owner","co_owner"]`; PATCH/DELETE menolak co-owner yang mengubah/menghapus anggota berperan owner (`getMemberRole` — hanya owner asli yang bisa); `findManageableMember` tetap melarang aksi kelola pada member owner; `ROLE_VALUES` + pesan error diperbarui. `/api/invitations` juga `["owner","co_owner"]`.
  - Halaman `members` & `settings` menerima owner/co-owner; `BottomNav`/`DesktopNav` menampilkan menu Anggota & Pengaturan untuk owner/co-owner; `canManage` transaksi & kategori ikut menyertakan `co_owner`.
  - UI halaman Anggota: opsi role "Co-owner" di select; opsi "Owner" hanya tampil untuk owner asli; select & tombol Hapus dinonaktifkan untuk member owner bila user bukan owner (`currentRole` dikirim dari server).
  - Undangan via email TETAP bendahara/viewer (co-owner diangkat via ubah peran) — konsisten dengan constraint tabel `invitations`.
- [x] Fitur: **pembatasan pembuatan organisasi (hanya owner)** — org switcher: item "Buat organisasi baru" hanya dirender bila user owner di salah satu org (`canCreateOrg` dihitung di `app/(dashboard)/org/[slug]/layout.tsx`); halaman `/onboarding`: user yang sudah tergabung tapi bukan owner di org mana pun ditolak `Forbidden` ("Hanya owner yang bisa membuat organisasi baru."); `create-organization-form.tsx` menampilkan pesan sama bila RLS menolak (defense-in-depth). User baru tanpa org tetap bisa buat org pertama (onboarding).
- [x] Fix PDF: **jarak & kesejajaran logo di header** — perjalanan: margin logo 64×64 mula-mula `[0,0,12,0]`, lalu `[0,8,16,0]` dan `[0,8,28,0]` — tapi tetap terlihat menempel. **GOTCHA pdfmake: `margin` pada node `image` di dalam `columns` DIABAIKAN** (khususnya margin kiri/kanan), jadi jarak yang diset di logo tidak pernah muncul. Solusi: jarak dipindah ke margin kiri kolom teks (`stack`) → `[16,0,0,0]`, dan logo diberi `margin-top 3` agar bagian atas gambar sejajar dengan huruf "L" di "Laporan" (margin-top 8 sebelumnya membuat logo terlihat lebih rendah dari teks). Hasil final di `lib/pdf/report.ts`: logo `{ image, width: 64, height: 64, margin: [0,3,0,0] }` + kolom teks `margin: [16,0,0,0]`.
- [x] Fix layout mobile (iPhone): **overlap & horizontal scroll** — (1) overlay petunjuk `dd/mm/yyyy` di `DateInput` kini disembunyikan di iOS via `supports-[-webkit-touch-callout:none]:hidden` (iOS memang menampilkan placeholder tanggal native; overlap terjadi karena overlay + teks native tampil bersamaan) + selector `::-webkit-datetime-edit-fields-wrapper` ikut di-`text-transparent`; (2) sel grid filter transaksi diberi `min-w-0` agar select/date input tidak meluap di layar sempit; (3) wrapper layout org diberi `overflow-x-clip` agar konten yang lebih lebar dari viewport tidak bikin scroll horizontal.

Catatan: perubahan role co-owner & pembatasan buat organisasi **mengubah skema/RLS** — dijalankan via migration baru `supabase/migrations/202608090001_co_owner_role_and_org_creation.sql` (file `03-database-migration.sql` yang sudah dijalankan TIDAK diubah).

## Operasional & Doc Sync (9 Agustus 2026)

- [x] **Migration co_owner dieksekusi ke Supabase production** — `supabase/migrations/202608090001_co_owner_role_and_org_creation.sql` dijalankan via Management API (`POST /v1/projects/snixmtiahvfuqxnokhkf/database/query`, dibungkus `BEGIN/COMMIT`, HTTP 201). Diverifikasi di DB: fungsi `can_create_organization()` ada; constraint `organization_members_role_check` = `('owner','co_owner','treasurer','viewer')`; policy `update_org_manage`, `insert/update/delete_member_manage`, `manage_invitations` aktif; `insert_org` = `created_by = auth.uid() AND can_create_organization()`; kategori & transaksi = `('owner','co_owner','treasurer')`; `delete_org_owner_only` tetap owner-only.
- [x] **Tooling: `scripts/run-sql.ps1`** — script untuk menjalankan migration/SQL dari lokal ke project Supabase: membaca `SUPABASE_ACCESS_TOKEN` dari `.env.local`, eksekusi via Management API, SQL dibungkus `BEGIN/COMMIT` (atomic). Pakai: `powershell -ExecutionPolicy Bypass -File scripts\run-sql.ps1 supabase\migrations\<file>.sql` (exit 0 + `HTTP 201` = sukses). `SUPABASE_ACCESS_TOKEN` dicatat di AGENTS.md, README, dan `.env.local.example`.
- [x] **Doc sync konsistensi role & export PDF** — perbaikan istilah yang tersisa di seluruh file md: (1) `01-rancangan-arsitektur-kas-platform.md`: poin MVP hapus istilah "admin" yang bukan role; skema `invitations` dikoreksi ke `('treasurer','viewer')` (undangan tidak menerima owner/co-owner); komentar RLS kategori/transaksi & halaman `members` jadi owner/co-owner/treasurer; komentar route `reports` jadi "semua role"; roadmap export PDF dicentang (Excel masih belum); (2) `02-requirements-prd.md`: acceptance criteria US-3.1/3.3/3.4/4.2 disesuaikan dengan owner/co-owner/treasurer & export semua role; (3) `README.md`: akurasi hint tanggal (iOS pakai placeholder native) + `SUPABASE_ACCESS_TOKEN` di daftar env vars. Entri changelog lama (yang menyebut owner/bendahara) sengaja dibiarkan sebagai catatan historis — ekspansi role tercatat di entri sesi ini.
- [x] **Rapikan penamaan dokumen** — `rancangan-arsitektur-kas-platform.md` di-`git mv` menjadi **`01-rancangan-arsitektur-kas-platform.md`** agar urutan nomor konsisten dengan `02`/`03`/`04`/`05` (arsitektur = dokumen sumber nomor 1). Seluruh referensi nama file di AGENTS.md, README.md, `02-requirements-prd.md`, dan entri changelog `05-task-breakdown.md` diperbarui.
- [x] **Audit akhir konsistensi semua referensi md** — pemeriksaan ulang seluruh dokumen; temuan & perbaikan di `01-rancangan-arsitektur-kas-platform.md`: (1) ilustrasi RLS section 5 disinkronkan dengan DB yang sudah berjalan — role `('owner','co_owner','treasurer')` untuk insert/update/delete kategori & transaksi, `insert_org` = `created_by = auth.uid() AND can_create_organization()`, `manage_invitations`/kelola member = `('owner','co_owner')`, `delete_org_owner_only` = owner only; (2) lengkapi policy yang belum ditampilkan: `update_categories`/`delete_categories`, `update_member_manage`/`delete_member_manage`, `delete_org_owner_only`, serta helper `can_create_organization()` dan RPC `is_slug_available`; (3) perbaiki konflik "organisasi aktif di React context + URL" → murni URL param (tidak ada React context di kode, konsisten 04-coding-standards); (4) hapus sebutan "magic link" (tidak diimplementasikan); (5) tambah `member-manage-dialog.tsx` di struktur komponen. Di `02-requirements-prd.md`: US-3.1 dipisah — form dicentang selesai, upload foto tetap `[ ]` (belum dikerjakan, `receipt_url` menganggur); heading "Kelola akun oleh owner" → owner/co-owner dengan target bendahara/viewer/co-owner; deskripsi hint tanggal diperbarui akurat (overlay hanya Android/desktop, iOS pakai placeholder native).
- [x] **Fitur: co-owner bisa dipilih langsung saat daftar anggota** (per konfirmasi user) — dropdown Peran di dialog "Daftarkan anggota manual" & "Tambah anggota existing" kini menampilkan Co-owner/Bendahara/Viewer; `createMemberSchema` & `addExistingMemberSchema` di `lib/types.ts` menerima `co_owner` (API POST `/api/members` tidak perlu berubah karena memakai role dari skema). Undangan via email TETAP bendahara/viewer (constraint tabel `invitations`). PRD & komentar skema `invitations` di arsitektur diperbarui (sebelumnya: "co-owner hanya via ubah peran").
- [x] **Fix horizontal scroll di popup "Daftarkan anggota manual"** — baris input "Password sementara" di dalam `flex gap-2` tanpa `min-w-0` sehingga input tidak bisa menyusut di bawah lebar intrinsiknya (input HTML `min-width: auto`) dan mendorong konten melebihi lebar popup di layar sempit → horizontal scroll. Solusi: `min-w-0 flex-1` pada input password (pola sama dengan fix sel filter transaksi sebelumnya).
- [x] **Fix lanjutan horizontal scroll di popup tambah anggota** — penyebab tersisa: `Button` punya `whitespace-nowrap`, sehingga dua tombol bertulisan panjang ("Email sudah punya akun? Tambahkan sebagai anggota existing" dan "Email sudah terdaftar — tambahkan sebagai anggota") tidak bisa wrap → mendorong konten melebihi lebar popup; karena popup `overflow-y-auto`, overflow horizontal ter-compute jadi scrollable. Solusi: kedua tombol diberi `whitespace-normal text-wrap` (link juga `text-left`, tombol emailExists `h-auto min-h-11`), dan kedua `DialogContent` diberi `overflow-x-clip` sebagai jaring pengaman.
- [x] **Fix label "Owner" tampil huruf kecil di dropdown peran (login co-owner)** — `SelectItem value="owner"` sebelumnya hanya dirender saat `isOwner`; saat login sebagai co-owner item itu tidak ada sehingga `SelectValue` base-ui menampilkan nilai mentah `member.role` (`"owner"` kecil) karena tidak ada label yang cocok. Solusi: item "Owner" selalu dirender tapi `disabled={!isOwner}` — label tampil kapital benar, dan co-owner tetap tidak bisa mengangkat owner lewat UI (API juga menolak). Verifikasi: aksi untuk member owner saat co-owner login memang sudah tidak ada — dropdown peran disabled, tombol Kelola & Hapus tidak dirender.
- [x] **Fix loading tombol berhenti terlalu cepat** di "Atur password baru" (`update-password`) dan "Buat organisasi" (`CreateOrganizationForm`) — `isSubmitting` dari react-hook-form berhenti begitu `onSubmit` selesai (langsung setelah `router.push` dipanggil), padahal navigasi ke halaman tujuan masih berjalan. Solusi: tambah state `redirecting` di-set `true` sebelum `router.push` (pola yang sudah dipakai di halaman login & register), tombol `disabled={isSubmitting || redirecting}` dan spinner/label bertahan sampai halaman tujuan dimuat.
- [x] **Fix overlap filter tanggal transaksi di iPhone** — di grid `grid-cols-2` mobile, "Dari tanggal" & "Sampai tanggal" berdampingan di sel sempit (~165px) sedangkan `input type="date"` iOS menampilkan segmen tanggal native (bulan/hari/tahun) dengan min-width besar sehingga konten meluap dan kedua input saling menimpa. Solusi: dua sel tanggal diberi `col-span-2` (full-width, satu baris penuh) di mobile dan `md:col-span-1` di desktop — layout desktop tidak berubah (tetap 4 kolom).
- [x] **Spacing field password di halaman login** — `CardContent` diberi `pb-2` supaya jarak bawah field password ke garis pembatas lebih lega; `pt-8` pada `CardFooter` sempat dicoba untuk menyamakan jarak atas tombol tapi akhirnya dihapus atas permintaan user — padding atas footer kembali ke default.

## Polish Animasi & Pengaturan Organisasi (9 Agustus 2026)

### Polish animasi (tanpa dependency baru, murni CSS/Tailwind + JS ringan)
- [x] **Polish 1: dialog & dropdown lebih lembut** — `duration-100` → `duration-200 ease-out` (overlay, `DialogContent`, `SelectContent`); popup dialog ditambah `slide-in-from-bottom-2` saat buka & `slide-out-to-bottom-2` saat tutup (feel naik turun halus).
- [x] **Polish 2: skeleton shimmer** — utility `.skeleton-shimmer` di `app/globals.css` (keyframe `skeleton-shimmer` + highlight `color-mix(var(--muted-foreground) 12%)` menyesuaikan tema terang/gelap); `components/ui/skeleton.tsx` beralih dari `animate-pulse` polos ke shimmer halus 1.8s.
- [x] **Polish 3: bottom nav feedback** — `transition-colors duration-200` pada item nav & tombol Keluar; ikon membesar halus saat aktif (`[&_svg]:scale-110`) dan mengecil saat ditekan pada tombol Keluar.
- [x] **Polish 4: item list feedback sentuh** — item list transaksi, kategori, & anggota diberi `transition-colors duration-200` + `active:bg-muted/40` (umpan balik tekan ringan, tidak mengubah perilaku klik).
- [x] **Polish 5: fade-in daftar transaksi** — daftar transaksi & empty state muncul dengan `animate-in fade-in-0 duration-300` setelah skeleton (ganti filter/halaman tidak lagi muncul mendadak).
- [x] **Polish 6: angka saldo count-up** — komponen client baru `components/animated-number.tsx` (requestAnimationFrame, 0 dependency, ease-out cubic 650ms) dipakai untuk saldo saat ini di dashboard; angka ter-update mengikuti nilai baru dari server tanpa animasi pada render pertama.
- [x] **Polish 7: transisi warna ganti tema** — utility `.theme-transitioning` di `globals.css` (transition `background-color`/`border-color`/`color` 300ms); `theme-picker.tsx` menambah class ini ke `<html>` saat ganti tema dan menghapusnya setelah 400ms — transisi hanya terjadi saat ganti tema, bukan saat interaksi lain.

### Fitur halaman Pengaturan organisasi (`org/[slug]/settings`)
- [x] **Ubah nama organisasi** (owner/co-owner) — form baru `components/org-name-form.tsx` (react-hook-form + `orgNameSchema` di `lib/types.ts`, pola manual `safeParse` seperti form lain); update `organizations.name` via client (RLS `update_org_manage`), `router.refresh()` agar header & org switcher ikut ter-update; notice sukses + pesan error manusiawi (termasuk tolak RLS).
- [x] **Ubah alamat (slug)** (owner/co-owner) — form baru `components/org-slug-form.tsx` (`orgSlugSchema`); cek ketersediaan via RPC `is_slug_available` (skip cek bila slug sama), tangani duplicate key, lalu `router.replace(/org/<slugbaru>/settings)` + refresh karena URL lama tidak berlaku. Hint memberitahu bahwa semua tautan lama ikut berubah.
- [x] **Hapus organisasi (owner only)** — `components/org-delete-button.tsx`: tombol merah "Hapus organisasi" + dialog konfirmasi wajib **mengetik ulang nama organisasi** (tombol hapus aktif hanya jika nama cocok); delete via client (RLS `delete_org_owner_only` menolak co-owner sebagai defense-in-depth), lalu `router.push("/")`. Tombol hanya dirender untuk role `owner`.
- [x] **Ringkasan anggota read-only** — card "Informasi organisasi" menampilkan nama, slug, tanggal dibuat, total anggota, dan jumlah per role (owner/co-owner/bendahara/viewer) yang dihitung di server (`organization_members` group by role).
- [x] **Halaman settings diperbarui** — tidak lagi placeholder: `app/(dashboard)/org/[slug]/settings/page.tsx` membaca `id, name, slug, created_at` + ringkasan anggota, tetap memblokir non owner/co-owner (`Forbidden`). Tidak ada perubahan skema/RLS (semua policy sudah ada).
- [x] **Perkuat intensitas animasi** (umpan balik user: "animasinya ga terlalu kerasa", diuji di prod) — durasi & jarak dinaikkan agar terlihat jelas tanpa terasa lambat: dialog overlay/popup `200→250ms` + slide `bottom-2→bottom-4` (32px); select `150→200ms`; skeleton shimmer kontras `12%→22%` + durasi `1.8→1.5s` + dikombinasi `animate-pulse` (breathing); bottom nav scale ikon aktif `110→125%` & durasi `200→300ms`; fade-in daftar transaksi ditambah `slide-in-from-bottom-2` & durasi `300→400ms`; count-up saldo `650→900ms`; transisi ganti tema `300→400ms`. Semua durasi non-standar memakai arbitrary value (`duration-[250ms]`/`duration-[400ms]`) karena bukan skala default Tailwind.
- [x] **Fix count-up saldo dashboard tidak terlihat saat buka halaman** (umpan balik user) — sebelumnya animasi hanya berjalan saat prop `value` berubah (mis. setelah tambah transaksi & kembali via navigasi client-side); saat page load penuh angka langsung tampil nilai akhir. Perbaikan di `components/animated-number.tsx`: tambah flag `startedRef` — pada mount pertama animasi berjalan dari 0 ke nilai akhir (900ms ease-out), pada perubahan nilai berikutnya animasi dari nilai sebelumnya ke nilai baru (perilaku lama tetap). Nilai 0 tidak beranimasi (langsung tampil).
- [x] **Fix header mobile: nama organisasi terpotong** (umpan balik user) — di HP header satu baris (logo + nama + org switcher + tema) melebihi lebar 375px sehingga nama org terpotong parah. Iterasi: (1) sempat dibuat **header 2 baris** (nama full-width di baris 1, switcher full-width di baris 2) tapi user menilai aneh karena nama org muncul 2× (baris 1 + di dalam switcher); (2) solusi pertama: **header 1 baris + org switcher jadi ikon `Building2` di mobile** (`SelectValue` di-`hidden md:block`, ikon `md:hidden`) — ternyata **gagal**: teks nama org masih tampil di trigger karena class `hidden` kalah dengan CSS trigger `*:data-[slot=select-value]:flex` (sama-sama set `display`, `flex` menang — elemen tetap terlihat, memakan ruang); (3) solusi final: `SelectValue` di-ganti **`max-md:sr-only`** — `sr-only` menyembunyikan via `clip`/1px (tidak bergantung `display`), jadi tak mungkin kalah oleh `*:data-...:flex`. Hasil: di mobile trigger hanya ikon gedung + chevron (kompak), nama org hanya tampil sekali di header full-width (`flex-1` truncate); desktop tetap menampilkan nama org di switcher.
- [x] **UX: feedback saat ganti organisasi** (umpan balik user) — sebelumnya setelah memilih org di switcher, layar tak berubah-ubah sampai selesai load (tidak ada indikasi proses). Solusi di `components/org-switcher.tsx`: saat org dipilih, trigger langsung berubah menjadi **spinner `Loader2 animate-spin` (mobile) / teks "Memuat…" (desktop)** dan `disabled` (tak bisa di-pilih lagi) sampai navigasi selesai. `isSwitching` dihitung derived: membandingkan `pendingPath` (URL tujuan) vs `pathname` terkini — begitu `pathname` mulai dengan tujuan (navigasi tuntas) atau `activeSlug` berubah, spinner hilang otomatis (tanpa `setState` dalam `useEffect`, sesuai aturan lint `react-hooks/set-state-in-effect`). Safety net: jika navigasi gagal, `setTimeout` 10 detik me-reset agar tidak stuck. Dipadukan dengan skeleton `org/[slug]/loading.tsx` yang sudah ada.
- [x] **Redesain header: nama aplikasi tampil + tema jadi ikon** (umpan balik user) — user sadar nama aplikasi (KasKita) tak pernah muncul di tampilan. Header `org/[slug]/layout.tsx` diubah: sebelah logo kini menampilkan **"KasKita"** (bukan nama org); karena nama org tidak lagi dobel, **org switcher kembali menampilkan label nama org** di semua ukuran (tetap ada spinner "Memuat…" saat ganti org); **ThemePicker jadi ikon saja** — trigger menampilkan ikon `Palette` (spinner saat menyimpan), `SelectValue` di-`sr-only` (label tema tetap tersedia untuk screen reader), chevron bawaan tetap tampil. Layout mobile 375px: logo + "KasKita" (flex-1) + switcher label (max-w-44 truncate) + ikon tema + logout (hidden md).
- [x] **Ikon gedung kembali di org switcher** (umpan balik user) — pada iterasi sebelumnya ikon `Building2` sempat dihapus saat label org dikembalikan; user menilai versi berikon lebih bagus. Ikon `Building2` dikembalikan di samping label nama org (semua ukuran layar), tetap berganti spinner saat sedang ganti organisasi.
- [x] **Perilaku navigasi seperti app native mobile** (umpan balik user) — dua hal: (1) **Ganti tab tidak menumpuk history**: `Link` di `components/bottom-nav.tsx` dan `components/desktop-nav.tsx` diberi prop `replace` — berpindah Dashboard → Laporan → Anggota tidak menambah riwayat, jadi tombol Back dari Anggota kembali ke Dashboard (tab terakhir yang "benar"), bukan ke Laporan. (2) **Back menutup dialog lebih dulu**: `Dialog` di `components/ui/dialog.tsx` (Root wrapper) kini saat terbuka melakukan `history.pushState({ dialogOpen: true })` + listener `popstate`; menekan Back (gesture Android/iOS) menutup dialog tanpa berpindah halaman — perilaku native. Saat dialog ditutup lewat tombol X/overlay/submit, entry history palsu dibersihkan lewat `history.back()`, tetapi hanya jika URL tidak berubah sejak dialog dibuka (guard `window.location.href === prevURL`) sehingga aman untuk kasus dialog yang ditutup bersamaan dengan navigasi (mis. hapus organisasi → `router.push("/")`). Dialog controlled maupun uncontrolled (`internalOpen`) ditangani; `onOpenChange` parent tetap dipanggil.
- [x] **Perbaikan perilaku tab: dashboard sebagai root** (umpan balik user) — versi pertama memakai `replace` di semua tab, akibatnya history hanya berisi 1 entry sehingga Back dari Anggota langsung menutup aplikasi ("malah ketutup"). Logika diperbaiki: **dashboard dianggap root**. Aturan di `bottom-nav.tsx` & `desktop-nav.tsx`: (a) Dashboard → tab lain = **push** (dashboard tetap tersimpan di stack); (b) tab lain → tab lain = **replace** (tidak menumpuk); (c) tab mana pun → Dashboard = **replace**. Hasil: Dashboard → Laporan → Anggota lalu Back ⇒ kembali ke Dashboard; Back dari Dashboard ⇒ keluar app (wajar). Deep-link langsung ke tab non-dashboard tetap aman (Back = keluar).
- [x] **Back menutup dropdown (Select) juga** (umpan balik user) — pola yang sama seperti Dialog diterapkan ke `Select` (Root) di `components/ui/select.tsx`: saat dropdown terbuka, `history.pushState` + listener `popstate`; Back menutup dropdown lebih dulu. Berlaku untuk semua dropdown (org switcher, tema, pilih kategori/peran/jenis, dsb.), controlled maupun uncontrolled (`internalOpen`), dan menumpuk rapi dengan dialog (dialog → select di dalamnya → Back menutup select dulu, Back lagi menutup dialog).
- [x] **Nama aplikasi di header diperbesar** (umpan balik user) — "KasKita" di samping logo diubah `text-sm` → `text-lg` (desktop `md:text-xl`), tetap `font-bold` + `truncate`, supaya terlihat seperti header app native (WhatsApp/Instagram) alih-alih sekadar teks kecil.

## Improvement UX Batch 1 (10 Agustus 2026)

Sasaran (disetujui user dari daftar saran audit MD): **empty state di semua list, toast notifikasi + Undo hapus, pull-to-refresh list transaksi** — semuanya tanpa dependency baru.

- [x] **Infrastruktur toast tanpa dependency** — `components/ui/toast.tsx`: `ToastProvider` (context) + `useToast()` + `ToastViewport` (fixed di bawah layar, `bottom-24` di mobile agar tidak tertutup bottom nav, `md:bottom-6` di desktop; animasi `slide-in-from-bottom-4`). API: `toast({ title, description, variant, duration, action: { label, onClick } })` → auto-dismiss (default 5s, bisa di-set per toast), `action` mengundang tombol aksi yang menutup toast saat diklik. Provider di-mount di `app/(dashboard)/layout.tsx` (server component hanya membungkus children dengan client component — tidak ada perubahan arsitektur). ID unik via `crypto.randomUUID()` (fallback `Date.now`+random).
- [x] **Empty state di semua list** — komponen bersama `components/empty-state.tsx` (ikon dalam lingkaran `bg-muted` + judul + deskripsi + aksi opsional, varian `compact`). Dipakai: **Transaksi** (`Receipt`) — bedakan dua kondisi: tanpa filter → judul "Belum ada transaksi" + tombol "Tambah transaksi" (hanya `canManage`); dengan filter aktif → judul "Tidak ada transaksi yang cocok"; **Kategori** (`ArrowUpCircle` pemasukan / `ArrowDownCircle` pengeluaran, `compact`); **Anggota** (`Users`); **Laporan rincian kategori** (`Receipt`, `compact`). **Perbaikan UX (umpan balik user): tombol "Reset filter" semula hanya muncul di empty state saat filter aktif tapi tidak ada hasil — user tidak menemukannya.** Kini tombol **Reset filter** (berikut teks "Filter aktif sedang menyaring transaksi.") selalu tampil di bawah baris filter selama ada filter aktif, apa pun hasil list-nya. Aksi "Reset filter" di empty state **dihapus** (umpan balik user: jadi dobel) — empty state dengan filter aktif kini tanpa aksi, teksnya mengarahkan ke tombol reset di atas.
- [x] **Toast + Undo hapus transaksi** — `confirmDelete` di `transactions-view.tsx` menangkap baris yang dihapus (`removed`) lalu setelah sukses memunculkan toast "Transaksi dihapus" (durasi 8 detik) dengan tombol **Urungkan**. Undo meng-insert ulang transaksi dengan `id` yang sama + seluruh kolom (`organization_id, category_id, type, amount, description, transaction_date, created_by`) via client (insert **tanpa** `.select()` — menghindari GOTCHA RLS RETURNING), lalu `router.refresh()`; sukses → toast "Transaksi dipulihkan", gagal → toast destructive dengan pesan manusiawi. Closure per-toast menangkap baris spesifik, jadi menghapus dua transaksi beruntun tetap mengembalikan baris yang benar. Bonus konsistensi: toast sukses untuk hapus kategori & hapus anggota (menggantikan notice inline untuk hapus anggota).
- [x] **Pull-to-refresh list transaksi** — komponen baru `components/pull-to-refresh.tsx` (0 dependency, murni touch events + `useTransition`): membungkus seluruh halaman Transaksi; saat `window.scrollY === 0` dan user menarik ke bawah, konten ikut bergeser (dengan resistensi 0.45x, maks 96px) + indikator spinner muncul; lepas di atas ambang 64px → `startTransition(() => router.refresh())` (spinner berputar di posisi tetap sampai refresh selesai, lalu fade-out). Detail teknis: listener `touchmove` di-attach native dengan `{ passive: false }` agar `preventDefault()` berhasil (React synthetic touch adalah passive — tidak bisa mencegah overscroll/scroll); tidak ada `setState` di dalam `useEffect` (aman aturan lint `react-hooks/set-state-in-effect`); gesture diabaikan saat sedang refresh/scroll di bawah atas halaman.
- [x] **Lint 0 error (5 warning lama RHF `watch()` yang sudah dikenal) + `npm run build` sukses.**

## Improvement UX Batch 2 (10 Agustus 2026)

Lanjutan dari daftar saran audit MD — disetujui user: **quick date filter, nama file PDF deskriptif, prefetch tab, kirim ulang undangan, PWA update prompt** (tanpa dependency baru).

- [x] **Quick date filter transaksi** — tombol preset rentang tanggal di bawah baris filter: **Hari ini / 7 hari / Bulan ini / 30 hari**. Helper baru `dateRangeForPreset` di `lib/utils.ts` menghasilkan `{ from, to }` ISO; klik memanggil `applyParams({ from, to }, null)` (pagination ikut reset). Tombol yang sedang aktif di-highlight (`variant` default); pemakaian preset otomatis memicu baris "Filter aktif" + tombol Reset filter yang sudah ada.
- [x] **Nama file PDF deskriptif** — file laporan sekarang `laporan-kas-<slug-org>-<tahun>-<bulan>.pdf` (sebelumnya tanpa nama org). `reports/page.tsx` kini memilih `name, slug` organisasi dan mengirim `orgSlug` ke `ReportsView`; `anchor.download` di client disamakan dengan `Content-Disposition` server (`lib/pdf` sudah memakai slug sejak awal — hanya client yang menimpa). Hasil: saat dibagikan ke WhatsApp, nama file langsung menunjukkan organisasi mana.
- [x] **Prefetch tab navigasi** — `Link` di `bottom-nav.tsx` & `desktop-nav.tsx` diberi `prefetch` sehingga payload RSC semua tab dimuat lebih awal; berpindah menu (Dashboard/Transaksi/Laporan/Anggota/Pengaturan) terasa instan tanpa spinner skeleton lama.
- [x] **Kirim ulang undangan** — undangan yang belum diterima (status `pending`, termasuk yang sudah kedaluwarsa) kini tampil di halaman Anggota pada section baru **"Undangan belum diterima"** (email + role + "Berlaku sampai <tanggal>" / "Kedaluwarsa" + tombol **Kirim ulang**). `GET /api/members` menambahkan `pendingInvitations` (dengan flag `expired` dihitung server-side — menghindari `Date.now()` di render yang ditolak lint `react-hooks/purity`); `POST /api/invitations` mode `resend` (badan `{ orgId, invitationId, resend: true }`): membaca undangan lama (harus `pending`, role/email dari DB bukan client), membuat undangan baru (token + masa berlaku baru), mengirim ulang email via `inviteUserByEmail`, lalu menandai undangan lama `expired`. Otorisasi tetap `["owner","co_owner"]`.
- [x] **PWA update prompt** — service worker `sw.js` v3 (cache `kaskita-v3`): **`skipWaiting` otomatis dihapus** dari `install` (sebelumnya SW baru langsung menguasai tanpa aba-aba), diganti listener `message` `SKIP_WAITING`. Komponen baru `components/pwa-update-prompt.tsx` (di-mount di root layout, tampil di semua halaman termasuk login): mendeteksi SW baru via `registration.installing`/`waiting` + `controller` yang sudah ada (tidak muncul saat pemasangan pertama), lalu menampilkan banner "Versi baru KasKita tersedia." + tombol **Muat ulang** yang mengirim `SKIP_WAITING` dan reload lewat `controllerchange` (guard ref `reloading`). `next.config.ts` menambah header `Cache-Control: public, max-age=0, must-revalidate` untuk `/sw.js` agar update terdeteksi tepat waktu.
- [x] **Lint 0 error (5 warning lama) + `npm run build` sukses.**

## Perbaikan bug (10 Agustus 2026)

- [x] **Fix: hapus kategori malah balik ke halaman Transaksi** (umpan balik user) �?" setelah konfirmasi hapus kategori, dialog menutup lalu aplikasi kembali ke halaman Transaksi (sebelumnya) alih-alih tetap di Kategori. Akar masalah: `router.refresh()` dipanggil bersamaan dengan `setDeleting(null)`, sedangkan dialog menutup via `history.back()` (mekanisme back-close dialog, `components/ui/dialog.tsx`) sehingga memicu `popstate`/`restore` di Next router �?" race membuat router membalikkan navigasi ke halaman sebelumnya. Solusi (di `components/categories-view.tsx` + `components/category-form-dialog.tsx`): **halaman Kategori tidak lagi memakai `router.refresh()` sama sekali** untuk tambah/edit/hapus. Daftar kategori diubah jadi state client (`useState(categories)` sebagai nilai awal; di-reset otomatis saat pindah halaman yang me-remount page). Hapus: filter `list` secara lokal (tanpa refresh, tanpa race). Simpan (dialog form): `CategoryFormDialog` menerima prop baru `onSaved(saved)` yang mengembalikan baris hasil; insert memakai `id` dari `crypto.randomUUID()` (tanpa `.select()` �?" menghindari GOTCHA RLS RETURNING) dan `CategoriesView` memperbarui `list` (append untuk tambah, map untuk edit). Tidak ada interaksi dengan history router �?" tidak ada lagi jalur balik ke Transaksi.
- [x] **Fix: toast putih menyatu dengan latar halaman** (umpan balik user) �?" toast memakai `bg-card` (putih) di atas latar halaman pastel sehingga hampir tidak terbaca. Di `components/ui/toast.tsx`, varian default diubah jadi **inverted**: `bg-foreground text-background` (chip gelap dengan teks terang, kontras tinggi di semua 5 tema yang semuanya berlatar terang) + border `border-background/20` + shadow lebih kuat; deskripsi `text-background/70`. Varian destructive tetap merah (`bg-destructive text-destructive-foreground`) tapi border diperjelas `border-destructive-foreground/30` dan deskripsi `text-destructive-foreground/80`.
- [x] **Fix: semua dropdown tidak bisa ke-set nilainya** (umpan balik user) �?" pilih bulan di Laporan tidak berubah, filter Jenis/Kategori di Transaksi tidak bisa dipilih, begitu juga ganti organisasi. Akar masalah (satu kelas dengan bug hapus kategori): back-close `Select` (`components/ui/select.tsx`) memakai `history.pushState` saat buka + `history.back()` saat tutup. `history.back()` memicu `popstate` yang bersamaan dengan `router.push` yang dipanggil dari `onValueChange` (filter transaksi via `applyParams`, bulan/tahun laporan via `setPeriod`, ganti organisasi di `org-switcher`) �?" race membuat Next router "membatalkan" navigasi sehingga nilai tidak terapkan. Solusi: **hapus mekanisme back-close dari `Select`** (dropdown cukup ditutup base-ui sendiri saat item dipilih / Esc / tap luar). `Dialog` tetap memakai back-close (back menutup modal seperti app native). Efek samping positif: bug "pilih Jenis di form kategori menutup dialog" ikut hilang karena tidak ada lagi `popstate` dari Select yang didengar listener dialog. File `lib/use-back-close.ts` (rencana hook bersama) dihapus karena tidak dipakai.
- [x] **Fix lanjutan: masih bounce + "popup lain ikut ketutup"** (umpan balik user) �?" replaceState saja belum cukup. Ditemukan akar KEDUA di `components/ui/dialog.tsx`: effect back-close bergantung `[currentOpen, handleOpenChange]`, dan `handleOpenChange` bergantung `onOpenChange` yang kerap **arrow inline** (mis. `onOpenChange={(open) => !open && setDeleting(null)}` di `transactions-view.tsx`). Identitas berubah tiap render parent → effect **jalan ulang setiap parent re-render saat dialog terbuka** → cleanup memanggil `history.back()` (memicu popstate) lalu setup memasang `pushState` + listener baru yang justru MENDENGAR popstate itu → dialog menutup sendiri, dan entry history palsu menumpuk (gejala: hapus transaksi yang masih memantul, dan "popup lain muncul lalu semua tertutup" saat tambah transaksi). Perbaikan: effect back-close kini dependensi **hanya `[currentOpen]`** (jalan sekali per buka/tutup); handler terbaru dipanggil lewat `handleOpenChangeRef` (ref di-update di `useEffect` tanpa dep — update ref saat render dilarang lint `react-hooks/refs`). Selain itu `router.refresh()` di `confirmDelete` (`transactions-view.tsx`) dan simpan form transaksi (`transaction-form-dialog.tsx`) ditunda dengan **setTimeout ganda** supaya refresh dijalankan SETELAH task `popstate` dari `history.back()` dialog selesai diproses Next router (tanpa ini refresh sinkron beradu dengan popstate → "restore" ke halaman sebelumnya). Dengan dua lapis ini: satu entry palsu per dialog, satu `back()`, popstate no-op, dan refresh tidak pernah tumpang-tindih dengan popstate.
- [x] **Fix: item transaksi yang dihapus baru hilang setelah toast/refresh** (keputusan user: **"tunggu server dulu"**) — `confirmDelete` tetap mempertahankan loading "Menghapus..." di popup sampai Supabase mengonfirmasi; begitu sukses, popup tutup DAN item langsung hilang dari daftar tanpa menunggu `router.refresh()`/toast. Implementasi `components/transactions-view.tsx`: state baru `removedIds` (Set id) + `visibleTransactions = transactions.filter(tx => !removedIds.has(tx.id))` dipakai untuk render daftar & cek kosong. Setelah sukses → `setRemovedIds` menambah id (bersamaan `setDeleting(null)`), jadi item menghilang saat popup mulai menutup; `undoDelete` (tombol Urungkan di toast) menghapus id dari `removedIds` sebelum `router.refresh()` sehingga item pulih seketika. `router.refresh()` (setTimeout ganda, tanpa race popstate) tetap dipertahankan untuk sinkronisasi server (saldo/halaman lain). Gagal → error tetap tampil di dalam popup.
- [x] **Investigasi "popup muncul lagi setelah fadeout" di produksi: dev & build produksi lokal BERSIH, produksi masih bermasalah** — dua babak instrumentasi log (`[DEBUG-DIALOG]`/`[DEBUG-TX]`, `components/ui/dialog.tsx` + `components/transactions-view.tsx`): (1) dev `localhost:3000` alur Tambah bersih; (2) `npm run build` lalu `next start -p 3001` (build produksi kode lokal, log `$env:TEMP/opencode/kaskita-prod-start.log`) untuk alur Tambah/Edit/Hapus — SEMUA bersih (tiap alur `formOpen=false` → CLEANUP `removedByPop=false` `hrefMatch=true` → `BACK()`; tidak ada OPEN kedua). Verifikasi: `origin/main` == local HEAD == `7568454`, dashboard Vercel menampilkan deploy produksi `7568454` (kode sudah ter-deploy benar). Log debug dihapus kembali dari kedua file. Kesimpulan sementara: kode sudah benar; penyebab paling mungkin di perangkat user = **cache browser/service worker yang masih menyajikan bundle lama** (SW `kaskita-v3` cache-first untuk `/_next/static/`, hard reload tidak melewati cache SW). **Belum terkonfirmasi** —   menunggu tes ulang di jendela incognito (tanpa SW/cache).
- [x] **Fix: "popup muncul lagi setelah fadeout" (flash dialog) TERPUJA di produksi** — kesimpulan sementara entry sebelumnya (cache/SW lama) TIDAK benar. Bukti: (1) verifikasi MD5 chunk produksi == build lokal (kode tidak basi); (2) kambuh ter-reproduksi di build produksi lokal `next start -p 3001` hanya dengan throttling DevTools, dan hanya pada aksi **Simpan** (yang memicu `router.refresh()`); aksi **Batal** (tanpa refresh) aman — pemicu = refresh RSC, bukan mekanisme back-close dialog. Mekanisme: `router.refresh()` sebelumnya dijadwalkan dengan `setTimeout(0)` sehingga request RSC-nya jatuh **tepat di jendela exit+hold popup dialog** (~500ms: exit animasi 250ms + hold sampai unmount; unmount tertunda bila `back()` dialog lambat karena payload entry sebelumnya ter-throttle). Re-render page di frame yang sama dengan unmount popup (popup punya layer GPU `backdrop-blur`) membuat popup "terlihat muncul lagi" sepersekian detik lalu hilang — artifact render, bukan elemen DOM baru (log DOM/animasi bersih: enter/exit sekali, tidak ada re-ADDED/ATTR). **Solusi** (`components/transactions-view.tsx` + `components/transaction-form-dialog.tsx`): refresh tidak lagi `setTimeout(0)`. `TransactionsView` menyediakan `scheduleRefresh` (prop `onSaved` ke form dialog; dipakai juga di `confirmDelete` & `undoDelete`) yang menunda refresh **700ms** (> exit+hold terpanjang teramati ~540ms) dan **menunda ulang** bila masih ada dialog terbuka (`formOpenRef`/`deletingRef`, di-sync via `useEffect` karena lint `react-hooks/immutability` melarang akses ref saat render). Log debug babak 3 dihapus dari `components/ui/dialog.tsx`, `components/transactions-view.tsx`, `components/transaction-form-dialog.tsx`. Terverifikasi di `127.0.0.1:3001` (throttle + disable cache, urutan Tambah/Edit/Hapus berulang) — flash hilang.
- [x] **Fix: field terakhir di halaman "Lupa password" & "Daftar" kurang jarak ke garis pembatas** (umpan balik user — pola sama dengan fix spacing login sebelumnya) — `CardContent` di `app/(auth)/reset-password/page.tsx` dan `app/(auth)/register/page.tsx` diberi `pb-2` (konsisten dengan `app/(auth)/login/page.tsx`), sehingga input terakhir tidak menempel ke `border-t` footer. Halaman `update-password` tidak disentuh karena strukturnya beda (tombol dibungkus div `px/pb-(--card-spacing)` sendiri).
- [x] **Keep-alive Supabase (anti pause 7 hari free plan)** — free plan Supabase mem-pause project setelah ±7 hari tanpa aktivitas DB/API. Solusi tanpa ubah kode app: workflow GitHub Actions `.github/workflows/keepalive.yml` berjalan **tiap 2 hari** (`schedule: cron "0 6 */2 * *"`, 06:00 UTC) melakukan `curl` langsung ke PostgREST (`/rest/v1/organizations?select=id&limit=1`, header `apikey` + Bearer anon key) — query tereksekusi di Postgres walau RLS mengembalikan array kosong, cukup me-reset timer. Trigger manual `workflow_dispatch` untuk tes. Nilai URL + anon key disimpan sebagai **GitHub repo secrets** `KASKITA_SUPABASE_URL` / `KASKITA_SUPABASE_ANON_KEY` (di-set via `gh secret set`, dibaca langsung dari `.env.local`; fine-grained PAT user harus diberi permission repository **Secrets: Read and write** — sebelumnya 403). Terverifikasi: `gh workflow run` → run `31381303406` **success** (11s). Pemakaian kuota GitHub Actions sangat kecil (×15/bulan, ~10s/run).
- [x] **Insiden: project Supabase ke-pause 27 Agu 2026 MESKIPUN keep-alive jalan sukses** — user laporkan app "tidak bisa dibuka" di HP. Diagnosa: Vercel `kas-kita-delta.vercel.app` sehat (307 → `/login` normal); **Supabase `snixmtiahvfuqxnokhkf` status `INACTIVE`** (ke-pause, host PostgREST tidak merespons — `HTTP 000` timeout; run keepalive Agu 27 gagal `exit code 6` = DNS tak ter-resolve, bukti pause terjadi antara Agu 25 06:55 dan Agu 27 17:30). Keep-alive lama 1x/2 hari TERBUKTI TIDAK CUKUP mencegah pause. **Pemulihan:** `POST /v1/projects/{ref}/restore` via Management API (HTTP 200) → status polling 8×: `COMING_UP` → `RESTORING` → `ACTIVE_HEALTHY`; PostgREST kembali `200`; app normal.
- [x] **Fix: keep-alive Ver-2 (perkuatan anti-pause)** — pelajaran dari insiden di atas + riset perilaku pause-detection Supabase 2026 (aktivitas yang dihitung = query Postgres sungguhan, bukan probe statis; cadence `setiap-N-hari` berisiko bolong karena GH Actions bisa skip/delay job terjadwal; `401/403` RLS-deny TETAP dihitung aktivitas karena SQL dieksekusi dulu). `.github/workflows/keepalive.yml` ditulis ulang: (1) **cron 4x sehari** `17 1,7,13,19 * * *` (menit ganjil hindari slot top-of-hour yang antri) — buffer besar di bawah threshold 7 hari; (2) SELECT ke **SEMUA tabel public** (`organizations organization_members categories transactions invitations`, list = kontrak, update kalau ada tabel baru) + header **`Prefer: count=exact`** agar PostgREST menambah query plan `count(*)` (aktivitas ter-dobel); (3) probe tambahan `auth/v1/settings` + `storage/v1/bucket` (belt-and-braces, best-effort non-fatal); (4) kriteria sukses aman: `2xx` ok, `401/403` ditaruh `::notice::` (bukan error), **gagal cuma `5xx`/`000`** (koneksi putus = project mati → workflow exit 1 biar sadar duluan). Gunakan kembali secret lama `KASKITA_SUPABASE_URL` / `KASKITA_SUPABASE_ANON_KEY`. Konfirmasi tambahan sebelum deploy: `gh workflow run` manual → harus sukses.

## Upload bukti foto transaksi — US-3.1 (30 Agustus 2026)

- [x] **Fitur: upload foto bukti transaksi (opsional)** — item PRD terakhir yang tadinya `[ ]`. Form transaksi kini punya field "Foto bukti (opsional)": pilih gambar (`accept="image/*"`, tombol h-11), **dikompres otomatis di client** (`lib/receipts.ts` `compressReceiptImage`: canvas, maks dimensi 1600px, JPEG quality 0.8 → file kecil seperti 100-300KB), pratinjau thumb 64px + tombol "Hapus pilihan"; saat edit tampil bukti lama (signed URL) dengan aksi "Ganti"/"Hapus foto"; file **TIDAK wajib** (transaksi tetap bisa disimpan tanpa foto).
- [x] **Penyimpanan: Supabase Storage bucket `receipts` (private)** — path `receipts/<organization_id>/<uuid>.jpg`; `receipt_url` di tabel `transactions` menyimpan path tersebut. Upload langsung via client (`storage.upload`, `upsert:false`) — TANPA `service_role` (tetap client RLS user, defense-in-depth).
- [x] **Migration baru `supabase/migrations/202608300001_receipt_storage_policies.sql`** (dijalankan via `scripts/run-sql.ps1`, HTTP 201, diverifikasi di `pg_policies` + `storage.buckets`): (1) `update storage.buckets` → `file_size_limit=5242880` (5 MB) + `allowed_mime_types` gambar (jpeg/png/webp/heic/heif) sebagai pengaman server-side; (2) policy RLS `storage.objects`: `receipts_select_org_member` (SELECT semua anggota org), `receipts_insert_org_manage` & `receipts_delete_org_manage` (INSERT/DELETE owner/co-owner/treasurer) — org diekstrak dari folder pertama path via `storage.foldername(name)[1]::uuid`, dipakai helper `is_org_member`/`get_org_role`. Isolasi data tetap di level database.
- [x] **Menampilkan bukti** — item list transaksi menampilkan chip "Lihat bukti" (pin `Paperclip`) untuk SEMUA role (viewer boleh lihat); klik membuka `components/receipt-view-dialog.tsx` (lightbox) yang memuat **signed URL** (`createSignedUrl` 1 jam) — bukan URL publik, file private tidak terekspos.
- [x] **Auto-delete anti file yatim** (mencegah limit storage 1 GB penuh oleh sampah): (1) **transaksi dihapus** → file ikut dihapus (`deleteReceipt`, best-effort setelah row terhapus — gagal hapus file tidak menggagalkan alur); catatan: undo hapus memulihkan baris transaksi TANPA foto (file sudah terlanjur dihapus = sesuai kata "dihapus permanen", `receipt_url` null); (2) **edit ganti/hapus foto** → file lama dihapus HANYA setelah update berhasil (kalau update gagal, bukti lama aman), dan file baru ikut dihapus bila update/insert gagal; (3) **organisasi dihapus** → `deleteOrgReceipts` (list folder `receipts/<org_id>` + remove semua) dijalankan SEBELUM hapus org; gagal → hapus org dibatalkan dengan pesan error.
- [x] **Type & query** — `TransactionRow` di `lib/types.ts` ditambah `receipt_url`; `transactions/page.tsx` select + normalisasi ikut memuat `receipt_url`.
- [x] **Error handling manusiawi** — validasi file di client (harus gambar, ≤15MB mentah, hint "maks 5 MB"); pesan upload error dipetakan (`uploadReceiptError`: RLS → "Kamu tidak punya izin...", exceed → "Ukuran file melebihi batas 5 MB"); server error form memakai `errorMessage` yang sudah ada.
- [x] **Lint 0 error (5 warning lama RHF `watch()` yang sudah dikenal) + `npm run build` sukses.** Perlu diingat: perbaikan lint `react-hooks/set-state-in-effect` di `receipt-view-dialog` memakai pola `key={receiptUrl}` pada child `ReceiptImage` (state fetch di-reset lewat remount, bukan setState sinkron di effect).

## Fitur pelacakan iuran per warga/unit — migration 202608300002 (30 Agustus 2026)

Keputusan desain (dikunci bersama user): **1 transaksi = 1 bulan iuran** (bayar multi-bulan dipecah jadi N transaksi agar saldo/rekap per bulan akurat); periode **boleh backdated**; pembayar = entitas generik `dues_payers` (label tampilan per organisasi, default "Warga"); kategori iuran dikenali via flag `is_dues` + nominal standar `dues_default_amount` (mulai sama untuk semua unit, ruang override tetap disiapkan); menu masuk = kartu Dashboard (mobile) + `DesktopNav` (desktop), bottom nav tetap 6 item.

- [x] **Migration `supabase/migrations/202608300002_dues_tracking.sql`** (file BARU, `03-database-migration.sql` & migration sebelumnya tidak diubah; user setuju mengubah trigger `create_default_categories` via `create or replace`): tabel **`dues_payers`** (+ `idx_dues_payers_org`, unique org+name, index `idx_transactions_dues_period`); `organizations.dues_entity_label` default `'Warga'`; `categories.is_dues` (not null default false) + `dues_default_amount` (nullable, check > 0); `transactions.dues_payer_id` + `dues_period` (date hari-1 bulan) + constraint `transactions_dues_pair_check` (kehadiran berpasangan) & `transactions_dues_income_check` (iuran hanya income); `organization_members.payer_id` (link akun → unit); RLS `dues_payers` = select `is_org_member`, insert/update owner/co-owner/treasurer, **tanpa policy delete** (nonaktif via `active=false` biar riwayat utuh); `create or replace function create_default_categories()` → kategori "Iuran Warga" dapat `is_dues=true`; 1× `UPDATE categories` untuk org existing. Dijalankan via `scripts/run-sql.ps1` → **HTTP 201**; diverifikasi di DB (kolom, `pg_policies` 3 policy, 2 check constraint, helper `is_org_member`/`get_org_role`/`create_default_categories` ada).
- [x] **Types** (`lib/types.ts`): `transactionSchema` + `dues_payer_id`/`dues_period`; `duesFieldsSchema` (payer + periode + jumlah bulan — validasi `dues_default_amount` harus sudah di-set); `payerSchema`; `orgDuesLabelSchema`; `categorySchema` + `is_dues`/`dues_default_amount`; `PayerRow`.
- [x] **Kategori**: toggle "Transaksi biasa / Iuran (per warga)" + field nominal standar di `category-form-dialog.tsx`; badge "Iuran" di `categories-view.tsx`; perbaikan teks izin (owner/co-owner/treasurer). Normalisasi `dues_payers` via `categoryFromEmbedded` di categories page.
- [x] **Form transaksi iuran** (`transaction-form-dialog.tsx` ditulis ulang): kategori iuran → dropdown "Dibayar oleh (label entity)" + "Periode bulan/tahun" (rentang currentYear−5 s.d. currentYear+2, backdated) + "Bayar untuk berapa bulan" (pecah jadi N records, nominal dipatok `dues_default_amount`, preview `N × Rp X = Rp Y`); auto-fill nominal standar; narrowing `const data = parsed.data` menyelesaikan nullable TS. `transactions-view.tsx`/`transactions/page.tsx`: tampil "nama payer · Bulan Tahun" di bawah kategori; select membawa `dues_payers(name)` + `organizations.dues_entity_label` + `payers`.
- [x] **Halaman Iuran** (`app/(dashboard)/org/[slug]/dues/page.tsx` + `components/dues-view.tsx`): tabel status per bulan (Belum/Cicil Rp x/Rp y/Lunas), nav bulan + tahun, ringkasan X/Y lunas + nominal masuk, riwayat per unit ("rumah saya" anchor + badge "Kamu"), toggle tampilkan unit nonaktif; target bulan = SUM `dues_default_amount` kategori iuran (kalau 0, ada bayar = lunas). Semua role bisa lihat; kelola hanya owner/co-owner/treasurer.
- [x] **Kelola unit pembayar** (`components/payer-manage-dialog.tsx`): tambah / ganti nama / nonaktifkan unit; mode "link akun" — daftar anggota via `GET /api/members`, checkbox (banyak-ke-satu via `organization_members.payer_id`), simpan via client supabase (RLS `update_member_manage`). **State reset memakai pola `key={manageSession}` dari `dues-view.tsx`** (remount saat dibuka) — menghindari lint `react-hooks/set-state-in-effect` tanpa me-remount saat animasi tutup.
- [x] **Entri menu & pengaturan**: `org-dues-label-form.tsx` + card di `settings/page.tsx` (owner/co-owner) untuk ubah label entitas; `desktop-nav.tsx` + item "Iuran" (icon ClipboardList); `dashboard/page.tsx` + kartu pintasan Iuran (muncul hanya bila ada unit aktif).
- [x] **Verifikasi**: `npx tsc --noEmit` clean; `npm run lint` **0 error** (5 warning lama RHF `watch()`); `npm run build` sukses — route baru `ƒ /org/[slug]/dues`. Docs di-sync: `01` (skema `dues_payers` + kolom baru + RLS + struktur + menu/nav), `02` (entri Fitur Tambahan, checklist), `05` (entri ini). PRD US-3.1 upload bukti ditandai selesai di entri sebelumnya.
- [x] **UX ulang alur iuran — nominal total & anti dobel catat** (ikut keputusan user): (1) **Nominal = jumlah total yang diterima**, bukan per bulan — helper `splitDues(total, perMonth)` → `{ full, rem }` di `transaction-form-dialog.tsx`; `plan`/`planCount` (derived dari `watch` + standar kategori) dipakai untuk preview (`4 × Rp 50.000 + cicil Rp 20.000 = Rp 220.000`), label tombol submit (`Menyimpan N transaksi...`), dan loop insert (N record bulan penuh ber-nominal `dues_default_amount` + 1 record cicilan `rem` di bulan berikutnya via `addMonths`); (2) **cap pecahan `MAX_DUES_MONTHS = 48`** (sebelumnya 12), tolak nominal setara > 48 bulan dengan pesan manusiawi; (3) **tahun di-autoselect tahun berjalan, bulan TIDAK** (prefill `"YYYY-"`; bulan dipilih bendahara manual); (4) **bulan yang sudah lunas untuk unit terpilih di-disable** di pemilih periode — daftar periode terbayar per unit (`paidPeriodsByPayer`) diagregasi di `transactions/page.tsx` dari `transactions` (filter `dues_payer_id`/`dues_period` tidak null, `dues_period.slice(0,7)`), dialirkan → `TransactionsView` → `TransactionFormDialog`; bulan lunas diberi label "(lunas)" + `disabled`; periode yang terlanjur berisi bulan lunas di-reset otomatis ke `"YYYY-"`; (5) field "Bayar untuk berapa bulan" jadi **display read-only** (hasil hitung sistem, bukan input). Field Nominal kembali selalu editable. Catatan: `duesFieldsSchema` (wajib `YYYY-MM`) tetap berlaku — submit menuntaskan `YYYY-MM-01` sebelum write DB (kolom `date`). Bukti hijau: `tsc` clean, lint 0 error, `next build` sukses.
- [x] **Fix dobel-catat & penempatan sisa yang benar** (temuan user): dulu sisa/cicilan pecahan selalu ditaruh di `addMonths(period, full)` — kalau bulan itu sudah lunas, jadinya dobel (mis. Agustus lunas + bayar 70rb "untuk Juli" → Juli 50rb + 20rb dobel ke Agustus = 70rb). Helper **`duesSchedule`** (`transaction-form-dialog.tsx`) kini amount-aware: (1) bulan **lunas** (total tercatat >= `perMonth`) dilewati, (2) bulan yang **baru cicil** DIDAHULUKAN ditutup sampai lunas, baru sisanya jatuh ke bulan kosong berikutnya. Contoh nyata user: Agustus lunas + bayar 60rb untuk Juli → Juli 50rb + September 10rb cicil; lalu bayar 70rb untuk Juni → Juni 50rb + 20rb **menutup cicilan September** (bukan Oktober); kalau September tertutup penuh baru sisa masuk Oktober. Data `paidPeriodsByPayer` diubah `Record<payer, string[]>` → `Record<payer, Record<"YYYY-MM", jumlah>>` (diagregasi `transactions/page.tsx` SUM `amount` per bulan). Pemilih bulan hanya mengunci bulan **lunas** (bukan yang baru cicil); efek reset periode hanya bulan lunas; pratinjau "Bayar N bulan" & label tombol pakai jumlah jadwal aktual (`dueCount` dari `schedulePreview`); cap 48 bulan dicek ganda (planCount + `schedule.length`); guard anti-loop 480 bulan. `tsc` clean, lint 0 error, `next build` sukses.
- [x] **UX: pemilih periode (bulan/tahun) baru bisa dibuka setelah pilih warga dulu** (ikut permintaan user, `transaction-form-dialog.tsx`): kedua `<Select>` periode diberi `disabled={!duesPayerId}` — urutan isian jadi warga → periode; hint di bawahnya dinamis ("Pilih warga dulu untuk memilih periode." vs "Bulan yang sudah lunas ... dikunci; bulan yang baru dicicil tetap bisa ditutup"). Prefill tahun berjalan tetap berjalan (nilai `"YYYY-"` mengisi saat kategori iuran dipilih; tahun tampil begitu unlock). Edit transaksi tidak terpengaruh (periode sudah terisi). `tsc` clean, lint 0 error, `next build` sukses.
- [x] **Halaman Iuran: breakdown per kategori** (ikut pilihan user — sebelumnya semua kategori iuran dijumlah jadi satu target/status sehingga tak kelihatan bayaran masuk kategori mana): `dues-view.tsx` kini menghitung `paidByCategory` (payer → kategori → nominal terbayar bulan terpilih) dan menampilkan **satu baris per kategori iuran** di tiap kartu unit (nama kategori + `Rp terbayar / Rp target` + chip Lunas/Cicil/Belum). `payableCategories` = kategori is_dues yang punya `dues_default_amount > 0` (target). **Status keseluruhan unit = lunas hanya bila semua kategori lunas**, cicil bila ada bayaran tapi belum semua lunas, belum bila kosong; kategori dengan nominal bebas (target null) tetap masuk total terbayar tapi tak punya chip status sendiri. `dues/page.tsx` tidak berubah (data sudah membawa `category_id` per transaksi). `tsc` clean, lint 0 error, `next build` sukses.

## Export PDF laporan iuran tahunan (30 Agustus 2026)

- [x] **Fitur: export PDF laporan iuran tahunan (semua role)** — tombol "Export PDF {tahun}" di halaman Iuran (pakai tahun terpilih di nav periode) mengunduh `laporan-iuran-<slug>-<tahun>.pdf`. Dihasilkan server-side di **`GET /api/reports/dues?orgId&year`** (pdfmake, mendukung pembayaran iuran yang terpecah lintas kategori). Isi PDF (landscape A4, header logo KasKita + judul/org/tahun/tanggal buat): **Ringkasan tahunan** (jumlah unit aktif, unit lunas sepanjang tahun, target tahunan, total iuran diterima, sisa belum terkumpul — hanya bila ada target), **Rekap per kategori iuran** (target/bulan, target/tahun, terkumpul, sisa), **Matriks unit × 12 bulan** (sel nominal: hijau tebal = lunas bulan itu, oranye ber-`*` = baru cicil, `—` = kosong; kolom Total per unit, angka hijau = lunas bila semua bulan capai target), dan **daftar "Belum Lunas per unit"** (bulan per bulan dengan cicil & sisa, untuk pengingat; fallback teks "Sudah lunas semua").
- [x] **Implementasi**: `lib/pdf/dues-report.ts` — module pdfmake mandiri (`generateDuesReportPdf({ orgName, entityLabel, year, payers, duesCategories, transactions })`, types `DuesReportPayer`/`DuesReportCategory`/`DuesReportTx`, `monthlyTarget` = SUM `dues_default_amount` kategori iuran). Route `app/api/reports/dues/route.ts` mengikuti pola `app/api/reports/route.ts` (auth `getRequester` roles owner/co-owner/treasurer/viewer, filter transaksi iuran `dues_payer_id` bukan null + `dues_period` dalam rentang tahun, `Cache-Control: no-store`). `dues-view.tsx`: tombol export (spinner + `disabled` saat proses, pesan error manusiawi bila gagal), fetch blob + auto-download (pola `reports-view.tsx`); `dues/page.tsx` meneruskan `orgSlug`.
- [x] **Verifikasi**: `npx tsc --noEmit` clean (gotcha: literal `alignment`/`width` di objek pdfmake harus `as const` karena konteks `.map()` unannotated melebarkan tipe), `npm run build` sukses — route baru `ƒ /api/reports/dues`. Docs di-sync: PRD (entri Fitur Tambahan) + changelog ini. Belum di-commit; biarkan user tes prod (include tahun berganti via nav periode).

### Fix hasil review user di prod (30 Agustus 2026)

- [x] **Target iuran tahunan salah** — sebelumnya `targetYear = monthlyTarget × 12` (hanya 1 unit), padahal yang dibandingkan dengan baris "Total iuran diterima" adalah nilai SEMUA unit dalam setahun. Sekarang `targetYear = monthlyTarget × 12 × payers.length` (unit aktif) sehingga "Target iuran tahunan" = jumlah warga × bulanannya, dan "Sisa belum terkumpul" konsisten. Rekap per kategori ikut disesuaikan: "Target / bln" & "Target / thn" × `payers.length` (organisasi seluruh unit), "Sisa" = target − terkumpul.
- [x] **Matriks unit × 12 bulan kepotong di kanan** — lebar kolom fixed (`[90, 12×44, 66]`) membuat tabel mengapit sebatas margin halaman. Diganti `[90, 12×"*", "auto"]` (pdfmake membagi ruang sisa ke kolom `*`, kolom total menyesuaikan konten) sehingga tabel selalu pas di dalam frame tanpa overflow; `pageSize: "A4"` eksplisit + `pageOrientation: "landscape"`.
- [x] **Rapi UI mobile toolbar Iuran** — nav periode (chevron/bulan/tahun) jadi baris terpisah; tombol "Export PDF {tahun}" + checkbox "Tampilkan nonaktif" pindah ke baris keduanya sendiri (`justify-between`, wrap aman di 375px); pesan error export menempati baris penuh di bawahnya. `tsc` clean, lint 0 error (5 warning lama), `next build` sukses.

### Fix: matriks pembayaran bisa dibedakan per kategori (30 Agustus 2026)

- [x] **Matriks jadi per warga × kategori** — sebelumnya sel matriks menampilkan TOTAL semua kategori per bulan, jadi kalau iuran punya >1 kategori tidak terlihat bayaran masuk kategori mana. Sekarang matriks berubah jadi **"Matriks Pembayaran per Bulan (per Kategori)"**: tiap warga tampil sebagai **baris band tebal ber-latar abu** (nama warga, `colSpan` 14), di bawahnya satu baris per kategori iuran (di-indent, tanpa ulang nama warga) dengan sel nominal per bulan, kolor hijau/oranye `*` dihitung terhadap **target kategori itu sendiri** (bukan total semua kategori); baris "Iuran lain (tanpa target)" muncul hanya bila ada bayaran yang tak masuk kategori bertarget; kolom Total = jumlah setahun per kategori per warga. Footnote diperjelas: "Satu baris = satu kategori iuran per warga… target tiap kategori ada di tabel Rekap." `tsc` clean (koreksi tipe `Map<string, number[]>` + param `.some`), `next build` sukses.
- [x] **Bagian "Belum Lunas per Warga" ikut dipecah per kategori** — sebelumnya masih agregat (seperti matriks lama): satu blok per warga RANGKUMAN SISA total semua kategori. Sekarang tiap blok warga menampilkan baris per kategori: `Iuran Warga: Jan 50.000, Feb (cicil 20.000, sisa 30.000), …`. Sekaligus diperbaiki hitungan ringkasan **"Warga lunas sepanjang tahun"** yang tadinya pakai total agregat (bisa salah label lunas saat semua uang masuk ke satu kategori) → sekarang benar-benar mengecek **setiap kategori × setiap bulan ≥ target kategori itu** (fallback "ada bayaran tiap bulan" bila tak ada kategori bertarget). `tsc` clean, `next build` sukses.
- [x] **"Belum Lunas" hanya menampilkan bulan yang sudah berjalan** (ikut umpan balik user — Juli misalnya baru bayar Januari tapi daftar belum lunas sudah bejejer s/d Desember): untuk laporan tahun berjalan loop "Belum Lunas" dibatasi sampai bulan berjalan (`elapsedMonths = now.getMonth()+1`), laporan tahun lampau tetap 12 bulan penuh. Ringkasan ikut menyesuaikan label: tahun berjalan → **"{Warga} lunas s.d. bulan berjalan"** dan pengecekan "lunas" hanya atas bulan yang sudah lewat; pesan kosong → "sudah lunas untuk semua bulan yang sudah berjalan di tahun X". `tsc` clean, `next build` sukses.

## Improvement kelola warga (30 Agustus 2026)

- [x] **Hapus pesan "menautkan warga hanya owner/co-owner"** — tombol "Akun" memang sudah disembunyikan untuk non-owner/co-owner, jadi teks itu redundant.
- [x] **Fix bug serius: menyimpan tautan satu warga menghapus link warga lain** (temuan user — Warga A dicentang X, Y, Z lalu simpan; buka Warga B, centang X, simpan → Y & Z di Warga A kehilangan centang). Penyebab: `saveLinks` meng-update `payer_id` SEMUA akun di daftar; akun yang tidak dicentang di tampilan B di-set `null` padahal sedang tertaut ke A. Solusi: `saveLinks` kini hanya menyentuh akun yang **berubah relatif posisi saat ini** — akun di-uncheck hanya di-null-kan bila ia memang tertaut warga yang sedang dibuka; akun yang tertaut warga lain TIDAK disentuh; akun yang dicentang dipindahkan ke warga ini.
- [x] **Indikator + konfirmasi tautan ke warga lain** (permintaan user): akun yang sudah tertaut warga lain menampilkan catatan amber **"Sudah tertaut ke {nama}"** di bawah emailnya; saat dicentang muncul konfirmasi `window.confirm` (". . . sudah tertaut ke X. Pindahkan ke Y?") — batal berarti centang dikembalikan. Posisi tautan disimpan di state `currentPayer` (user_id → payer_id) yang diisi dari `organization_members` tiap kali mode "Akun" dibuka; nama warga diambil dari prop `payers`. `tsc` clean, `next build` sukses.

## Improvement laporan + hapus warga (30 Agustus 2026)

- [x] **Laporan iuran ikut serta warga nonaktif yang punya riwayat di tahun itu** (ikut umpan balik user — "kalau warga beneran nunggak tapi belum pernah bayar kenapa disembunyikan?"): `DuesReportPayer` kini membawa `active`; query route mengambil SEMUA payer (tanpa filter `active`). **Roster laporan** = warga `active=true` (termasuk yang belum pernah bayar, supaya yang menunggak tetap ketahuan) **+** warga nonaktif yang punya ≥1 transaksi iuran di tahun itu; warga nonaktif tanpa transaksi tahun itu tidak dimunculkan. Nama warga nonaktif diberi label **"(nonaktif)"** di matriks & daftar belum lunas (helper `displayName`). Target tahunan & rekap kategori dihitung dari **jumlah warga AKTIF** (`activeCount`), bukan roster (warga nonaktif tak menambah target). Footnote matriks ditambah penjelasan label "(nonaktif)". `tsc` clean, `next build` sukses.
- [x] **Tombol hapus warga di Kelola Warga + konfirmasi tegas** (menutup lubang "salah tambah tak bisa dihapus"): migration **`supabase/migrations/202608300003_payer_delete_policy.sql`** menambah policy DELETE `dues_payers` untuk owner/co-owner/treasurer + `grant delete` (dijalankan via `scripts/run-sql.ps1` → **HTTP 201**). UI: tombol **Hapus** ikon (merah, 44px, per warga) hanya berhasil untuk warga **tanpa transaksi iuran**; kalau sudah punya riwayat diblokir dengan pesan manusiawi "Nonaktifkan saja agar riwayat tetap tersimpan" (Postgres sendiri menolak via check `transactions_dues_pair_check` karena FK `on delete set null` mengosongkan `dues_payer_id` sementara `dues_period` tersisa — warga ber-riwayat memang tak mungkin terhapus). Konfirmasi memakai **AlertDialog baru** `components/ui/alert-dialog.tsx` (Base UI `@base-ui/react/alert-dialog`, tak menutup via klik luar/Escape) berjudul "Hapus warga {nama}?" dengan tombol merah "Hapus" (spinner saat proses) + "Batal"; dijelaskan bahwa aksi permanen & tautan akun ikut dilepas (FK `organization_members.payer_id` set null). `payerTxCount` dihitung di `dues-view.tsx` dari `txByPayer` (seluruh riwayat iuran) lalu diteruskan ke dialog; keberhasilan hapus men-trigger `onChange` agar daftar hilang tanpa reload. `tsc` clean, lint 0 error (5 warning lama), `next build` sukses.
- [x] **Fix "Periode tidak valid" saat EDIT transaksi iuran** (temuan user — bendahara salah input nominal lalu mau perbaiki): akar masalahnya bukan nominal, tapi kolom `dues_period` — DB menyimpan date penuh `"YYYY-MM-DD"` sedangkan form & `duesFieldsSchema` (`lib/types.ts`) memakai `"YYYY-MM"` (regex `^\d{4}-\d{2}$`). Pretfill edit mengisi nilai mentah `"2026-06-01"` sehingga validasi submit selalu gagal → setiap edit transaksi iuran (apapun perubahannya) kena "Periode tidak valid". Perbaikan dua lapis: (1) saat reset form untuk edit, `dues_period` dinormalisasi `.slice(0, 7)` → select periode langsung menampilkan bulan yang benar; (2) di submit, normalisasi defensif `length === 10 → slice(0,7)` sebelum `duesFieldsSchema.safeParse`, lalu konversi balik jadi `-01` saat menyimpan. `tsc` clean, `next build` sukses.

## UX transaksi: feedback saat refresh setelah simpan/hapus (30 Agustus 2026)

- [x] **Daftar transaksi tampak "tak berubah" setelah tambah/edit/hapus** (temuan user — padahal di belakang sedang `router.refresh()`): `scheduleRefresh` memanggil `router.refresh()` langsung sehingga `isPending` (dari `useTransition`) tidak pernah menyala → daftar tidak menunjukkan proses apa pun sampai data baru tiba. Perbaikan: `router.refresh()` dibungkus `startTransition` (pola resmi Next.js untuk loading indicator saat refresh) → `isPending` menyala, daftar berubah jadi `TransactionListSkeleton` + `aria-busy` + `sr-only "Memuat..."` selama RSC baru diambil. Berlaku untuk tambah, edit, hapus, dan undo-delete; 700 ms delay + gate popup (`formOpenRef`/`deletingRef`) tetap dipertahankan agar tidak kambuh gejala popup "muncul lagi" di jendela exit+hold. `tsc` clean, lint 0 error, `next build` sukses.